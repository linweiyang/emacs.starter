(require-package 'htmlize)
(require-package 'ox-reveal)
(require 'ox-reveal)

;;(setq org-reveal-root "https://cdn.jsdelivr.net/reveal.js/3.0.0/")
(setq org-reveal-mathjax t)

(provide 'init-wylin-reveal)
